# Demo is fully local and does not contact the defense/simulator.
VITE_DASHBOARD_MODE=demo

# Once a dashboard backend implements GET /api/dashboard returning DashboardData,
# use "api" and start that backend on http://127.0.0.1:8090.
# VITE_DASHBOARD_MODE=api
